import React from "react";
import Header from "./Header";
import Hero from "../ui/Hero";

const Layout = () => {
  return (
    <div>
      <Header />
      <Hero />
    </div>
  );
};

export default Layout;
